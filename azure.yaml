# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: covid-tracker
services:  
  app:
    language: dotnet
    project: ./CovidTracker.AppHost/CovidTracker.AppHost.csproj
    host: containerapp
